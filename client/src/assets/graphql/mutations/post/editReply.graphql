mutation editReply($id: Int!, $text: String) {
    editReply(cmt_idx: $id, text: $text) {
        cmt_idx
        text
        reg_dt
        upt_dt
        commenter {
            nick_nm
            user_idx
        }
    }
}