mutation writeReply($board: Int!, $user: Int!, $nick: String!, $text: String) {
    writeReply(board_idx: $board, user_idx: $user, nick_nm: $nick, text: $text) {
        cmt_idx
        user_idx
        text
        nick_nm
        reg_dt
        upt_dt
    }
}