mutation writeReply($board: Int!, $user: Int!, $text: String, $ip: String!) {
    writeReply(board_idx: $board, user_idx: $user, text: $text, reg_ip: $ip) {
        cmt_idx
        commenter {
            user_idx
            nick_nm
        }
        text
        reg_dt
        upt_dt
    }
}