query UserProfile($id: Int!) {
    user(user_idx: $id) {
        user_nm
        user_profile
        nick_nm
        dpt_cd
        admin_type
        user_type
        articles {
            title
            body
            board_idx
        }
    }
}