query UserProfile($id: Int!) {
    user(user_idx: $id) {
        user_nm
        nick_nm
        dpt_cd
        admin_type
        user_type
        articles {
            title
            body
            board_idx
        }
    }
}