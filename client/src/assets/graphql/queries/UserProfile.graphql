query UserProfile($id: Int!) {
    user(user_idx: $id) {
        user_nm
        user_profile
        nick_nm
        dpt_cd
        admin_type
        user_type
        articles {
            title
            category_idx
            board_idx
            view_cnt
            reg_dt
        }
    }
}